// Random JavaScript code generated by AI Assistant

function getRandomNumber(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

function getRandomColor() {
  const colors = ["red", "green", "blue"];
  return colors[getRandomNumber(0, colors.length)];
}

function getRandomShape() {
  const shapes = ["circle", "square", "triangle"];
  return shapes[getRandomNumber(0, shapes.length)];
}

function getRandomWord() {
  const words = [
    "apple",
    "banana",
    "cherry",
    "date",
    "elderberry",
    "fig",
    "grape",
    "honeydew",
    "kiwi",
    "lemon",
    "mango",
    "nectarine",
    "orange",
    "passionfruit",
    "peach",
    "plum",
    "quince",
    "raspberry",
    "strawberry",
  ];
  return words[getRandomNumber(0, words.length)];
}

function getRandomFood() {
  const foods = ["pizza", "sushi", "taco", "salad"];
  return foods[getRandomNumber(0, foods.length)];
}
